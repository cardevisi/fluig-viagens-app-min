Pensando no fluxo aula
    Proposta - requer validação
        - Cada um tem o seu próprio repositório, para criarmos toda experiência de criação de pipeline.

    Vai clonar o repositório?
        - Deixamos na máquina
    
    Vai construção do projeto Fluig?
        Já pronto?

    Podemos deixar os arquivos do pipelinee do projeto com lacunas para ele ir preenchendo, mas previamente criados.
    Podemos deixar no projeto as funções utilitárias prontas.
    E focamos somente no que faz parte na aula. 

    Como vamos criar a contas no github?
        - Criar 30 contas do gmail e deixar previamente configurada em cada máquina.
        - Criar projeto no github para cada aluno.
        - Criar email de recuperação de senha.
        - Criar senha padrão para todos.

        - Clonar repo via https 
        - Criar aluno repositório  
        - Faz a config do remote
        - Criar dataset completo e fazer push

    Iniciamos a construção do pipeline 
        fluig-deploy.yml
        Faz push do yml
        Quebra o pipeline 
        Configurar secrets
        Roda manual com as secrets, sucesso.
    
    Ambiente do Fluig
        - Ambiente Fluig remoto no cloud.
         - Cada um com seu usuário se senha (TODOS ADMINS)
         - Acessa painal de controle
         - Datasets
         - Procurar pela datasets com o nume do aluno.
